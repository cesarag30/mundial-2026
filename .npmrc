# Ionic React + React 18 + react-scripts 5 a veces tiene conflictos menores
# de peer dependencies. Esta opción los acepta como "warning" en lugar de error.
# Es la configuración estándar para proyectos Ionic React modernos.
legacy-peer-deps=true
